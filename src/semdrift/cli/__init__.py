# semdrift.cli — Command-line interface.
#
# This module will eventually expose commands such as:
#     semdrift scan .
#
# The CLI orchestrates the other modules (scanner, parser, model,
# detection, reporting) rather than containing their implementation.
